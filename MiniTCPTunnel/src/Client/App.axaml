<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fluent="clr-namespace:Avalonia.Themes.Fluent;assembly=Avalonia.Themes.Fluent"
             x:Class="MiniTCPTunnel.Client.App">
  <!--
    애플리케이션 전역 리소스를 담는 루트.
    현재는 기본 스타일만 사용하지만, 추후 테마/리소스 확장을 고려해 분리한다.
  -->
  <Application.Styles>
    <fluent:FluentTheme />
  </Application.Styles>

  <!--
    트레이 아이콘 정의:
    - 창을 닫아도 앱을 종료하지 않고 트레이로 숨기기 위한 진입점이다.
    - 메뉴로 창 열기/종료를 제공한다.
  -->
  <TrayIcon.Icons>
    <TrayIcons>
    <TrayIcon ToolTipText="MiniTCPTunnel"
              Icon="avares://MiniTCPTunnel.Client/Assets/TrayIcon.png">
      <TrayIcon.Menu>
        <NativeMenu>
          <NativeMenuItem Header="창 열기"
                          Click="OnTrayOpenClick" />
          <NativeMenuItem Header="종료"
                          Click="OnTrayExitClick" />
        </NativeMenu>
      </TrayIcon.Menu>
    </TrayIcon>
      </TrayIcons>
  </TrayIcon.Icons>
</Application>

