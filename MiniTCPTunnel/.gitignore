# Build outputs from dotnet build/run (generated binaries and intermediates).
bin/
obj/

# Visual Studio and IDE caches (per-user, machine-specific state).
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
.idea/

# Test result outputs (generated by test runners).
TestResults/

# Local secrets or private keys (never commit).
*.key
*.pem
*.pfx

# OS-generated junk files.
.DS_Store
Thumbs.db
