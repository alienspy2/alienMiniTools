namespace UnityEngine
{
    public enum ForceMode
    {
        Force,
        Acceleration,
        Impulse,
        VelocityChange
    }

    public enum ForceMode2D
    {
        Force,
        Impulse
    }
}
