#version 450

// Empty fragment shader for depth-only shadow pass.
// Veldrid requires a fragment shader even for depth-only rendering.

void main()
{
    // Depth is written automatically by the rasterizer
}
